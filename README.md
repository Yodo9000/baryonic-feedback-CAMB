The Jupyter Notebooks with the Python code for my bachelor's project in Astronomy.

Description of files:
- `code_main.ipynb` has the code to setup and run the simulations.
- `plots_and_mc_progress.ipynb` has code to generate plots from output files generated by `code_main.ipynb`, and lets you track the progress of the MCMC while it is running.
